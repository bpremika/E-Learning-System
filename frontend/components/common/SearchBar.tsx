import { TextInput, Input } from "@mantine/core";

interface Props {
    onSearchValueChange?: (newValue: string) => void;
    search: string;
}
const SearchBar = ({ search, onSearchValueChange }: Props) => {
    return <></>;
};
export default SearchBar;
